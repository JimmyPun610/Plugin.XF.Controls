<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="Plugin.XF.Controls.Controls.AnnotatedEntry">
  <ContentView.Content>
        <StackLayout>
            <ContentView Content="{Binding Path=Entry}"/>
            <Label x:Name="AnnotationLabel" FontSize="Small" Text="{Binding Path=Annotation}" TextColor="{Binding Path=AnnotationColor}" IsVisible="{Binding Path=IsShownAnnotation}"
                   Margin="8,0,0,0"/>
        </StackLayout>
    </ContentView.Content>
</ContentView>